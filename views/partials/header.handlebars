<div class="nav-container">
    <nav class="nav">

        {{!-- LOGO --}}
        <a href="/" aria-label="Sabka Bazar homepage">
            <img src="/static/images/logo.png" alt="Sabka bazar logo" class="nav__logo">
        </a>

        {{!-- PAGE LINKS --}}
        <div class="nav__left">
            <ul class="nav__page-links" aria-label="Page links">
                <li><a href="/">Home</a></li>
                <li><a href="/products">Products</a></li>
            </ul>
        </div>

        {{!-- CART AND USER AUTH LINKS --}}
        <div class="nav__right">

            {{!-- USER AUTH LINKS --}}
            <ul class="nav__user-auth-links" aria-label="Authentication links">
                <li><a href="/login">Sign In</a></li>
                <li><a href="/register">Register</a></li>
            </ul>

            {{!-- CART --}}
            <div class="cart">
                <button class="cart__btn" id="cart-btn" aria-label="cart" aria-describedby="cart-items-count">
                    <svg version="1.1" id="Layer_1" focusable="false" xmlns="http://www.w3.org/2000/svg"
                        xmlns:xlink="http://www.w3.org/1999/xlink" x="0px" y="0px" viewBox="0 0 24 24"
                        style="enable-background:new 0 0 24 24;" xml:space="preserve" class="cart__btn-icon">
                        <path
                            d="M7 18c-1.1 0-1.99.9-1.99 2S5.9 22 7 22s2-.9 2-2-.9-2-2-2zM1 2v2h2l3.6 7.59-1.35 2.45c-.16.28-.25.61-.25.96 0 1.1.9 2 2 2h12v-2H7.42c-.14 0-.25-.11-.25-.25l.03-.12.9-1.63h7.45c.75 0 1.41-.41 1.75-1.03l3.58-6.49A1.003 1.003 0 0 0 20 4H5.21l-.94-2H1zm16 16c-1.1 0-1.99.9-1.99 2s.89 2 1.99 2 2-.9 2-2-.9-2-2-2z" />
                    </svg>

                    <span class="cart__btn-items-count" id="cart-items-count">0 items</span>
                </button>
            </div>
        </div>

        <article id="cart" class="">
            <div class="cart__overlay" id="cart-overlay"></div>
            <div class="cart__container" id="cart-container">

                {{!-- CART HEADER --}}
                <header class="cart__header">
                    <h2 class="cart__heading">My Cart (<span id="cart-item-count">0</span> items)</h2>
                    <button class="cart__close-icon" id="cart-close-icon" aria-label="Close Cart">&#10006;</button>
                </header>

                {{!-- CART ITEMS --}}
                <div class="cart__items-list-container">
                    <ul class="cart__items-list" id="cart-list">

                    </ul>
                    <div class="cart__lowest-price" id="cart-lowest-price">
                        <img src="/static/images/lowest-price.png" alt="">
                        <p>You won't find it cheaper anywhere</p>
                    </div>
                    <div class="cart__items-empty-msg" id="cart-empty-msg">
                        <h3>No items in your cart</h3>
                        <span>Your favourite items are just a click away</span>
                    </div>
                </div>

                <footer class="cart__footer">
                    <p class="cart__promo-msg" id="cart-promo-code">Promo code can be applied on payment page</p>
                    <button class="btn btn--primary btn--full" id="cart__footer__proceed-btn">
                        <span>Proceed to Checkout</span>
                        <span id="cart-total">Rs.167 > </span>
                    </button>

                    <button class="btn btn--primary btn--full" id="cart__footer__start-shopping-btn">
                        <a href="/products">Start Shopping</a>
                    </button>
                </footer>
            </div>
        </article>
    </nav>
</div>
<script defer src="/static/js/header.js"></script>
<script defer src="/static/js/cart.js"></script>